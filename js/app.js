(function() {
	var app = angular.module('LFViewer');
    /*
     * It's possible I'm doing this wrong, but I'll need to access a list of properties throughout all of these tabs, so I'm putting initialization logic and storing stuff in the root scope.
     */
	app.run(function($rootScope) {
	});
})();